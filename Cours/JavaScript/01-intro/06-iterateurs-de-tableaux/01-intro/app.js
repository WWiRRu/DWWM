// iterateurs d'array
// forEach, map, filter, find, reduce
// itere dans un array - pas besoin de boucle for
// accepte une fonction callback en argument, appelle le callback à chaque iteration. 
const numbers = [1, 2, 3, 4, 5];

//* affiche les nombres
for (let index = 0; index < numbers.length; index++) {
    console.log(numbers[index]);

}